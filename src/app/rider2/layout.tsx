import Rider2LayoutClient from './Rider2LayoutClient'

export default function RiderLayout({ children }: { children: React.ReactNode }) {
  return <Rider2LayoutClient>{children}</Rider2LayoutClient>
}
